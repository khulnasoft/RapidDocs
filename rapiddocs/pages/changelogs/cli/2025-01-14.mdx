## 0.48.0
**`(feat):`** Adds support for nullable types in the Rapiddocs definition, such as the following:

```yaml
types:
  User:
    properties:
      name: string
      email: nullable<string>
```


