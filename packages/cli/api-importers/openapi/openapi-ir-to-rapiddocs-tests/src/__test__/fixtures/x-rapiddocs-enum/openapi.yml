openapi: 3.1.0
info:
  title: Test extension `x-rapiddocs-enum` for enums
  version: 1.0.0
paths: {}
components: 
  schemas: 
    SpeechModel:
      type: string
      description: The speech model to use for the transcription.
      x-rapiddocs-sdk-group-name: transcripts
      enum:
        - nano
      x-rapiddocs-enum:
        nano:
          name: Nano
          description: The Nano tier is a lightweight model that is optimized for speed and cost.