## 0.15.1
**`(chore):`** _It's been forever since we released a non release candidate!_

**Break**
- The file structure of the Rapiddocs folder has now changed. If you have a single API, your definition can live directly at the top-level. If you have multiple, they will need to live in an apis folder. When you run `rapiddocs upgrade` the directory structure will automatically be updated. 

