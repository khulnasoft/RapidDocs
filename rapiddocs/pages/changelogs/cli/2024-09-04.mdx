## 0.41.0
**`(feat):`** Adds generic object declarations to the rapiddocs definition. Now we can define generics and
use them in alias declarations to minimize code duplication:

```yml
types:
  GenericTest<T>:
    properties:
      value: T
      other-value: string

  GenericApplication:
    type: GenericTest<string>
```

More information can be found here: https://buildwithrapiddocs.com/learn/api-definition/rapiddocs/types#generics.


