## 0.50.17
**`(fix):`** Improve performance of `rapiddocs docs dev` by only reloading the markdown content when
only markdown files are changed, avoiding unnecessary recompilation of the full docs.


