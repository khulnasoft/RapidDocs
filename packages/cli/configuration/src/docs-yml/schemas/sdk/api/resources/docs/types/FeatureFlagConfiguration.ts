/**
 * This file was auto-generated by Rapiddocs from our API Definition.
 */

import * as RapiddocsDocsConfig from "../../../index";

export type FeatureFlagConfiguration =
    /**
     * The name of the feature flag, We assume that it is a boolean feature flag that needs to be evaluated to true. */
    | string
    /**
     * A single feature flag with a configurable name, fallback value, and match value. Especially useful for non boolean feature flags. */
    | RapiddocsDocsConfig.FeatureFlag
    /**
     * A list of feature flags. If any of the feature flags are satisfied, we will show all content. */
    | RapiddocsDocsConfig.FeatureFlag[];
