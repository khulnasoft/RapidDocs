## 0.41.9
**`(internal):`** Adds a `bundle-path` hidden parameter for `rapiddocs docs dev` for use with `rapiddocs-platform` testing. You can pass the
path on the command line as an optional parameter.


