## 0.43.6
**`(fix):`** The OpenAPI importer now appropriately brings in responses that are under the `text/event-stream`
Content-Type if your endpoint is annotated with `x-rapiddocs-streaming`.
If your endpoint is not annotated with `x-rapiddocs-streaming`, then the response will be ignored.


