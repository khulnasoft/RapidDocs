/**
 * This file was auto-generated by Rapiddocs from our API Definition.
 */

import * as RapiddocsDocsConfig from "../../../index";

export interface FontConfig {
    /** If the `name` is not supplied, Rapiddocs will default it to a generated name that will be used to reference your custom font in the eventually injected CSS in the docs. */
    name?: string;
    /** The relative path of the font file. To define multiple weight and style variations of the same font, use `paths` instead. */
    path?: string;
    /** @default: `100 900`. */
    weight?: RapiddocsDocsConfig.FontWeight;
    /** @default: `normal`. */
    style?: RapiddocsDocsConfig.FontStyle;
    /** Use this instead of `path` if you want to specify multiple font files for different font weights and styles. */
    paths?: RapiddocsDocsConfig.FontConfigPath[];
    /** @default: `swap`. */
    display?: RapiddocsDocsConfig.FontDisplay;
    /** Define fallback fonts in case the custom font fails to load. */
    fallback?: string[];
    fontVariationSettings?: string;
}
