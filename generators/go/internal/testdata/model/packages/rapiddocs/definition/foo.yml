# Simple test for generating a Rapiddocs definition with multiple packages.
imports:
  extra: extra.yml
  bar: ./bar/bar.yml
types:
  Foo:
    properties:
      name: string
      value: extra.Value
      bar: bar.Bar
